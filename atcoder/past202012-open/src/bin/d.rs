use proconio::marker::*;
use proconio::*;
use std::cmp::*;
use std::collections::*;

#[fastout]
fn main() {
    input! {
        n: usize,
        s: [Chars; n],
    }

    let mut ans = n;

    println!("{}", ans);
}
